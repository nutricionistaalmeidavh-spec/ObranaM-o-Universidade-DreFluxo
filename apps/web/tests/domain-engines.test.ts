import { describe, expect, test } from 'vitest';import { financeEngine, executiveEngine, routeAssistantQuestion, productionEngine } from '../backend/domain-engines';
describe('domain engines',()=>{test('financial overdue obligations become deterministic critical alert',()=>{const r=financeEngine({inflowCents:100000,outflowCents:50000,suggestionsCount:1,expected:{openCents:30000,overdueCents:10000}});expect(r.metrics.overdue).toBe(10000);expect(r.alerts.some(a=>a.code==='FINANCE_OVERDUE'&&a.severity==='critical')).toBe(true)});test('assistant router selects relevant deterministic domains',()=>{const route=routeAssistantQuestion('Compare PIX, folha e atrasos do cronograma da obra');expect(route.domains).toContain('finance');expect(route.domains).toContain('rh');expect(route.domains).toContain('planning');expect(route.readOnly).toBe(true)});test('executive ranking is derived from engine alerts',()=>{const p=productionEngine({employees:[{attendance:'absent'},{attendance:'absent'},{attendance:'absent'}]}),f=financeEngine({expected:{overdueCents:5000}}),x=executiveEngine([p,f]);expect(x.ranking[0].domain).toBe('finance');expect(x.ranking[0].score).toBeGreaterThan(x.ranking[1].score)})});
